"""Command-line interface for Occulus.

Provides the ``occulus`` CLI tool with subcommands for point cloud
inspection, filtering, classification, conversion, DEM generation,
registration, and spatial tiling.

Usage::

    python -m occulus <subcommand> [options]

See ``python -m occulus --help`` for available subcommands.
"""
